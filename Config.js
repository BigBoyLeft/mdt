module.exports = {
    playerAuthToken: true, // if true Generate a secure auth token for each client to connect with the server with | if false the server with generate 1 auth token that will be shared accross all clients
};
